codecov:
  bot: "codecov"
  require_ci_to_pass: true
coverage:
  round: down
  range: 90..100
  status:
    project:
      default:
        target: 90%
        threshold: 0%
        branches:
          - main
          - dev
        if_ci_failed: error
comment:
  layout: "diff,files"
  behavior: default
  require_changes: false
